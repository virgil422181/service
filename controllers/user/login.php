<?php/* @var $user User *//* @var $session Session */if (isset($_POST['login'])){        $username = $_POST['username'];    $password = $_POST['parola'];    $loggedId = $user->checkCredentials($username, $password);    if($loggedId) {        $session->login($loggedId->id);        redirectTo(WEB_ROOT);    } else {        $errMessage = "Username-ul sau parola sunt gresite!";    }}$loginPage = include_once 'views/user/login.php';return $loginPage;